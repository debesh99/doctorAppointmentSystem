# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/test_db
spring.datasource.username=root
spring.datasource.password=Dp99@gmail
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate configuration
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=create

# Server configuration
server.port=8080

# Logging configuration
logging.level.root=INFO

# Other application properties
#In a Spring Boot application, the spring.jpa.show-sql property is used to configure whether SQL statements
 
#generated by the JPA (Java Persistence API) provider should be printed in the application